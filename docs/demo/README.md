# Demo Assets

Commit these files here before publishing the site:

- `streaming.gif` — ~10s recording of a streaming chat response (use Peek or Byzanz, keep under 5 MB)
- `models.png` — screenshot of the Models → Local tab
- `thinking.gif` — ~8s recording of a thinking block expanding and collapsing

Until these files exist, the demo gallery on the landing page shows broken image placeholders.
